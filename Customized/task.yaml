name: customized
command: './main.ps1 -LocalRepoURL {{localRepoURL}} -RunAsUser {{runAsUser}}'
inputs:
  localRepoURL:
    defaultValue: ''
    type: 'string'
    required: false
    description: | 
      Use the private winget repository.